#include <stdio.h>
#include <stdlib.h>
#define SIZE 5


int printFrequency(int* array, int size);

int main()
{
	int array[] = {5,6,7,6,5};
	printFrequency(array, SIZE);
	return 0;
}

int printFrequency(int* array, int size)
{
	value = array[0];

}
